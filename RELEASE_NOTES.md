# 思维导图编辑器 v2.1.0

本次更新新增「节点自由调整大小」：把鼠标移到节点右下角即可拖拽改变宽度，复盘时可按内容长短灵活控制每个节点。手机版与 PC 源码均在本仓库。

## ⬇️ 下载

- 下载附件 **`MindMapEditor-win32-x64.zip`**
- 解压整个 zip → 进入 `MindMapEditor-win32-x64` 文件夹 → 双击 **`MindMapEditor.exe`** 运行
- 适用 Windows 10 / 11（64 位），免安装、完全离线
- 首次运行若 SmartScreen 提示“未知发布者”，选「更多信息 → 仍要运行」（未购买代码签名证书）

## ✨ 新增

- **节点自由调整大小**：鼠标悬停或选中节点时，右下角出现斜纹手柄，按住左键左右拖动即可改变节点宽度（范围 70–600px），**文字自动换行**。
- 采用左上角固定、向右扩展；调整后**同级节点自动让位**，不会重叠。
- 宽度**随导图保存**，刷新或重新打开依然生效；导出 PNG 时按新宽度绘制。
- 触摸设备（平板等）自动隐藏手柄，避免误触。

## 📦 此前已具备

- **v2.0.0**：空白处自由文本便签、序号注释（导出 PNG 底部自动汇总）、词汇解释 / 加粗下划线悬浮气泡、节点文字颜色、右键重命名导图、右键双击编辑、修复导出 PNG 重复保存框。
- 多导图本地存储与标签管理、单画布多根节点、标签联想、自由连接（可拖拽走线 / 重接 / 写批注）、节点 7 色底色、框选多选、右键菜单、选择 / 平移 / 文本三模式、自定义快捷键（冲突提醒）、夜间模式、Markdown / Mermaid / 高清 PNG 导出。

数据完全保存在本机（localStorage），可用导出功能备份。
