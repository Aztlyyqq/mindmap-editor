# 思维导图编辑器 v2.3.0

本次更新把「导出」入口移到顶部导航栏，将「词汇解释」与「序号注释」融合为统一标注编辑器（序号可选、便签内也能标注词汇），并让图表数据编辑与图表类型切换双向联动、收紧数据页空白。

## ⬇️ 下载

- 下载附件 **`MindMapEditor-win32-x64.zip`**
- 解压整个 zip → 进入 `MindMapEditor-win32-x64` 文件夹 → 双击 **`MindMapEditor.exe`** 运行
- 适用 Windows 10 / 11（64 位），免安装、完全离线
- 首次运行若 SmartScreen 提示“未知发布者”，选「更多信息 → 仍要运行」（未购买代码签名证书）

## ✨ v2.3.0 新增

- **导出移到导航栏**：顶部导航栏新增 📤 按钮（Ctrl+E），设置弹窗里的导出项移除，右键菜单导出保留。
- **统一标注编辑器**：右键节点 / 便签 / 图片 / 图表 →「标注 / 解释」，一个弹窗同时管理词汇标注与整体说明。
  - 词汇标注：给文本里的词或短语加解释、选加粗 / 下划线；**便签（自由文本）里现在也能做词汇标注**。
  - 整体说明：可对整个对象写说明（图片、图表等无文本对象用这个）。
  - 每条标注都能勾选「添加序号」、**也可以不加**；编号自动递增、可手动改。
- **图表数据编辑联动**：点「✎ 数据」打开时类型与图表同步；在数据页切换图表类型实时联动到画布图表，保存时回写类型。
- **数据页 UI 优化**：画布高度按类型自适应，删除数值下方大面积空白，统计区间距收紧。
- PNG 导出同步：词汇序号进入底部“序号注释”汇总，便签按分段渲染加粗 / 下划线 / 序号。

## 📦 此前已具备

- **v2.2.0**：解释显示开关、节点 / 文本 / 图片 / 图表统一可选中可互相连接、空白右键插入图片、画布内嵌可视化图表、右上角设置齿轮、修复 `.pt` 下划线不连续。
- **v2.1.0**：节点右下角手柄拖拽调整宽度、文字自动换行、宽度随导图保存。
- **v2.0.0**：空白处自由文本便签、序号注释（导出 PNG 底部汇总）、词汇解释 / 加粗下划线悬浮气泡、节点文字颜色、右键重命名、右键双击编辑、修复导出 PNG 重复保存框。
- 节点复制 / 剪切 / 粘贴（可跨导图）、Markdown 一键转导图、可视化表格与统计、多导图本地存储、标签联想、自由连接（走线 / 重接 / 批注）、节点 7 色底色、框选多选、右键菜单、选择 / 平移 / 文本模式、自定义快捷键（冲突提醒）、夜间模式、Markdown / Mermaid / 高清 PNG 导出。

数据完全保存在本机（localStorage），可用导出功能备份。
