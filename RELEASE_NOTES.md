# 思维导图编辑器 v2.0.0

本次为 PC 桌面版的一次大更新，新增「空白便签 / 序号注释 / 词汇解释 / 文字颜色」等复盘常用能力，并修复了导出图片的重复弹窗问题。手机版与 PC 源码均在本仓库。

## ⬇️ 下载

- 下载附件 **`MindMapEditor-win32-x64.zip`**
- 解压整个 zip → 进入 `MindMapEditor-win32-x64` 文件夹 → 双击 **`MindMapEditor.exe`** 运行
- 适用 Windows 10 / 11（64 位），免安装、完全离线
- 首次运行若 SmartScreen 提示“未知发布者”，选「更多信息 → 仍要运行」（未购买代码签名证书）

## ✨ 新增

- **空白处自由文本便签**：顶栏新增「📝 文本」模式，在画布空白处**按住左键拖出一片区域**即可放置文本框，直接输入；支持拖动、双击编辑、右键删除，内容随导图自动保存。
- **序号注释**：节点和便签都能挂**编号角标**并填写解释，鼠标悬浮角标查看；编号在当前导图内自动递增（可手改）；**导出 PNG 时会在图片底部自动汇总“序号 + 解释”列表**。
- **词汇解释 / 标注**：给节点文本里的指定词汇添加解释，并设置**加粗 / 下划线**样式；鼠标悬浮该词弹出深色解释气泡（仿卡牌关键词），导出 PNG 时样式保留。
- **节点文字颜色**：右键节点可在 8 种文字颜色中选择，支持多选批量设置、一键恢复默认，与 7 种节点底色相互独立。
- **右键直接重命名导图**：在画布空白处右键 →「重命名本导图」即可改名（双击顶部标签的方式保留）。
- **右键双击节点**即可直接进入文字编辑（左键双击 / F2 / 右键菜单同样可用）。

## 🔧 改进

- 新增子节点 / 同级节点 / 新分支时，**画布保持当前视图，不再自动平移跳动**（切换导图、删除导图时仍会自动适应窗口）。
- 词汇解释弹窗的示例文案由卡牌专属示例改为通用示例。

## 🐛 修复

- **修复“下载 PNG 弹出两个保存窗口”的问题**：渲染层改为 Blob URL 下载、主进程改为单次系统保存框，现在每次导出**只弹出一次**保存对话框。
- 修复文本框选中状态与序号命令的一处时序问题。

## 📦 此前已具备

多导图本地存储与标签管理、单画布多根节点、标签联想、自由连接（可拖拽走线 / 重接 / 写批注）、节点 7 色底色、框选多选、右键菜单、选择 / 平移 / 文本三模式、自定义快捷键（冲突提醒）、夜间模式、Markdown / Mermaid / 高清 PNG 导出。

数据完全保存在本机（localStorage），可用导出功能备份。
